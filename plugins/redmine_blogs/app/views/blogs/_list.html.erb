<% if blogs.empty? %>
<p class="nodata"><%= l(:label_no_data) %></p>
<% else %>

<div id="blogs">
  <% blogs.each do |blog| %>
  <%= div_for(blog) do %>
  <div class="box">
    <% if (image = blog.head_image()) && image.present? %>
    <div class="image">
      <a href="<%= url_for :action => 'show', :id => blog %>"><img src="<%= download_named_attachment_url(image, image.filename) %>"></a>
    </div>
    <% end %>
    <div class="content">
      <h3><%= link_to blog.title, :action => 'show', :id => blog %></h3>
      <div class="wiki">
	<%= blog.short_description %>
      </div>
      <p class="author"><%= authoring blog.created_on, blog.author %></p>
    </div>
  </div>
  <% end %>
  <% end %>
</div>
<% end %>
